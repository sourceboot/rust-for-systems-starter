//! sqlkit — a reader for the SQLite file format, written one lab at a time.
//!
//! Each lab adds a module here with a `pub mod` line, and the project text tells you
//! when. Stage 01 starts you off: `warmup.rs` is already on disk, and rustc will not
//! look at it until you declare it below.
